Reference link: https://developer.hashicorp.com/terraform/install

steps:
1. download terraform keyrings  and save to /usr/share/keyrings
     " wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg"
2. download terraform package and attach to apt source list
    "echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list"
3. install the terraform by using apt
    "sudo apt update && sudo apt install terraform"
4. check it is installed or not 
    " terraform -v"

Errors:
error 1:
        wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
        --2026-06-26 06:26:59--  https://apt.releases.hashicorp.com/gpg
        sudo: 'gpg': command not found
        Resolving apt.releases.hashicorp.com (apt.releases.hashicorp.com)... 143.204.204.43, 143.204.204.40, 143.204.204.26, ...
        Connecting to apt.releases.hashicorp.com (apt.releases.hashicorp.com)|143.204.204.43|:443... connected.
        HTTP request sent, awaiting response... 200 OK
        Length: 3980 (3.9K) [binary/octet-stream]
        Saving to: ‘STDOUT’
    solution:
        - check gpg is available or not
            gpg --version
        - if not available install install
            "sudo apt install -y gnupg2"




2. Install Azure cli on ubuntu
    ```
        curl -fsSL 'https://azurecliprod.blob.core.windows.net/$root/deb_install.sh' | sudo bash
    ```